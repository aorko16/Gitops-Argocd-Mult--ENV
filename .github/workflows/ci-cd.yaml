# name: CI/CD Pipeline

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     steps:

#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Login to DockerHub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build and Push image
#         uses: docker/build-push-action@v4
#         with:
#           context: .
#           push: true
#           tags: |
#             aorko16/myapp:latest
#             aorko16/myapp:${{ github.sha }}

#       - name: Deploy to Dev
#         run: |
#           git config user.email "ci@github.com"
#           git config user.name "GitHub Actions"
#           sed -i "s|image: aorko16/myapp:.*|image: aorko16/myapp:${{ github.sha }}|g" apps/dev/deploy.yml
#           git add apps/dev/deploy.yml
#           git commit -m "Deploy ${{ github.sha }} to dev"
#           git push

#   promote-to-qa:
#     runs-on: ubuntu-latest
#     needs: build-and-push
#     if: success()
#     steps:

#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Promote to QA
#         run: |
#           git config user.email "ci@github.com"
#           git config user.name "GitHub Actions"
#           sed -i "s|image: aorko16/myapp:.*|image: aorko16/myapp:${{ github.sha }}|g" apps/qa/deploy.yml
#           git add apps/qa/deploy.yml
#           git commit -m "Promote ${{ github.sha }} to qa"
#           git push

#   promote-to-prod:
#     runs-on: ubuntu-latest
#     needs: promote-to-qa
#     if: success()
#     environment: production
#     steps:

#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Promote to Prod
#         run: |
#           git config user.email "ci@github.com"
#           git config user.name "GitHub Actions"
#           sed -i "s|image: aorko16/myapp:.*|image: aorko16/myapp:${{ github.sha }}|g" apps/prod/deploy.yml
#           git add apps/prod/deploy.yml
#           git commit -m "Promote ${{ github.sha }} to prod"
#           git push
